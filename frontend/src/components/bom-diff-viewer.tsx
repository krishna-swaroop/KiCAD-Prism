// BOM diff viewer — full implementation lives in feature/bom-diff branch.
export function BomDiffViewer(_props: Record<string, unknown>) {
    return (
        <div className="flex items-center justify-center h-full text-sm text-muted-foreground">
            BOM viewer coming soon
        </div>
    );
}
