// Gerber viewer — full implementation lives in feature/gerber-pdf-viewer branch.
export function GerberViewer(_props: Record<string, unknown>) {
    return (
        <div className="flex items-center justify-center h-full text-sm text-muted-foreground">
            Gerber viewer coming soon
        </div>
    );
}
